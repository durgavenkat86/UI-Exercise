export const MAX_PRODUCTS_SIZE: number = 4;

export const icon: { [key: string]: string } ={
 close: String.fromCodePoint(215),
 check: String.fromCodePoint(10003),
 plus:  String.fromCodePoint(10011),
 search: String.fromCodePoint(128270)
}
export const EMPTY_SPACE_REGEX: RegExp = /\s+/g;
